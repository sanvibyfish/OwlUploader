# 09. Finder 风格 UI 设计规范

## 概述

OwlUploader 采用 macOS Finder 风格的界面设计，提供熟悉、直观的文件管理体验。本文档定义了 UI 组件、交互模式和视觉规范。

---

## 1. 设计理念

### 1.1 核心原则

| 原则 | 描述 |
|------|------|
| **原生体验** | 遵循 macOS Human Interface Guidelines |
| **熟悉感** | 模仿 Finder 的交互模式，降低学习成本 |
| **一致性** | 统一的动画、颜色、间距规范 |
| **响应式** | 适应不同窗口尺寸，流畅的动画反馈 |

### 1.2 设计参考

- macOS Finder 文件浏览器
- SF Symbols 图标系统
- macOS 原生控件样式

---

## 2. 视图模式系统

### 2.1 支持的视图模式

| 模式 | 图标 | 快捷键 | 描述 |
|------|:----:|:------:|------|
| **表格 (Table)** | `tablecells` | `Cmd+1` | 带可排序列头的详细视图 |
| **图标 (Icons)** | `square.grid.2x2` | `Cmd+2` | 网格布局，显示缩略图 |

### 2.2 ViewModeManager

```swift
/// 视图模式管理器
@MainActor
class ViewModeManager: ObservableObject {
    @Published var currentMode: FileViewMode      // 当前模式
    @Published var iconSize: IconSize             // 图标大小 (48/64/96/128pt)
    @Published var showPreviewPanel: Bool         // 预览面板
    @Published var showFileExtensions: Bool       // 显示扩展名
}
```

### 2.3 图标尺寸

| 尺寸 | 大小 | 适用场景 |
|------|:----:|----------|
| 小 | 48pt | 高密度显示 |
| 中 | 64pt | 默认 |
| 大 | 96pt | 图片浏览 |
| 特大 | 128pt | 预览模式 |

### 2.4 组件文件

| 文件 | 职责 |
|------|------|
| `ViewModeManager.swift` | 模式管理、偏好持久化 |
| `FileGridView.swift` | 图标网格布局 |
| `FileGridItemView.swift` | 网格项（缩略图+文件名） |
| `FileTableView.swift` | 表格布局（列头+行） |

---

## 3. 工具栏设计 (FinderToolbar)

### 3.1 三区布局

```
┌─────────────────────────────────────────────────────────────┐
│ [← ↑] [↻]  │      [🔍 搜索...        ]      │ [📁+] [↑] [≡] [排序] [视图] │
│   导航区     │           搜索区              │         操作区              │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 导航区

| 按钮 | 图标 | 快捷键 | 功能 |
|------|:----:|:------:|------|
| 后退 | `chevron.left` | `Cmd+[` | 返回上一个访问的目录 |
| 前进 | `chevron.right` | `Cmd+]` | 前往下一个目录（后退后可用） |
| 返回上级 | `chevron.up` | `Cmd+↑` | 导航到父目录 |
| 刷新 | `arrow.clockwise` | `Cmd+R` | 重新加载列表 |

### 3.3 搜索区

- 居中显示，最大宽度 280pt
- 实时过滤，输入即搜索
- 清除按钮 (`xmark.circle.fill`)

### 3.4 操作区

| 按钮 | 图标 | 功能 |
|------|:----:|------|
| 新建文件夹 | `folder.badge.plus` | 创建新目录 |
| 上传 | `arrow.up.doc` | 选择文件上传 |
| 筛选 | `line.3.horizontal.decrease` | 类型筛选菜单 |
| 排序 | `arrow.up.arrow.down` | 排序方式菜单 |
| 视图切换 | 分段控件 | 切换视图模式 |

### 3.5 按钮样式

```swift
struct ToolbarButtonStyle: ButtonStyle {
    // 尺寸：28×22pt
    // 圆角：4pt
    // 悬停：灰色背景 (opacity 0.2)
    // 禁用：opacity 0.5
}
```

---

## 4. 路径导航 (PathBar)

### 4.1 设计规范

```
┌─────────────────────────────────────────────────┐
│ 🪣 my-bucket  >  📁 images  >  📁 2024         │
└─────────────────────────────────────────────────┘
```

### 4.2 交互行为

| 操作 | 行为 |
|------|------|
| 点击路径段 | 跳转到对应目录 |
| 悬停 | 高亮显示 |
| 根目录 | 显示存储桶图标和名称 |

### 4.3 组件规范

- 高度：28pt
- 分隔符：`chevron.right` (SF Symbol)
- 字体：系统字体 12pt
- 背景：`windowBackgroundColor`

---

## 5. 选择系统 (SelectionManager)

### 5.1 选择模式

| 模式 | 触发方式 | 行为 |
|------|----------|------|
| **单选** | 点击 | 选中当前，清除其他 |
| **切换** | `Cmd+点击` | 切换选中状态 |
| **范围** | `Shift+点击` | 选中锚点到当前的所有项 |
| **添加** | 拖拽选择 | 添加到现有选择 |

### 5.2 API 接口

```swift
@MainActor
class SelectionManager: ObservableObject {
    @Published var selectedItems: Set<String>    // 选中项集合

    func select(_ key: String, mode: SelectionMode, allKeys: [String])
    func isSelected(_ key: String) -> Bool
    func clearSelection()
    func selectAll(_ keys: [String])
    func invertSelection(_ allKeys: [String])
}
```

### 5.3 视觉反馈

| 状态 | 样式 |
|------|------|
| 未选中 | 透明背景 |
| 悬停 | 灰色背景 (opacity 0.06) |
| 选中 | 蓝色背景 (primary opacity 0.12) + 边框 |

---

## 6. 导航历史管理 (NavigationHistoryManager)

### 6.1 功能概述

类似 Finder 和浏览器的前进/后退导航，记录用户访问的目录历史。

```
后退栈 ← [当前路径] → 前进栈
```

### 6.2 API 接口

```swift
@MainActor
class NavigationHistoryManager: ObservableObject {
    @Published var canGoBack: Bool         // 是否可后退
    @Published var canGoForward: Bool      // 是否可前进

    /// 导航到新路径（清空前进栈）
    func navigateTo(_ path: String)

    /// 返回上一个位置
    func goBack() -> String?

    /// 前进到下一个位置
    func goForward() -> String?

    /// 清空所有历史
    func clearHistory()

    /// 重置到初始状态
    func reset(to initialPath: String)
}
```

### 6.3 交互行为

| 操作 | 快捷键 | 行为 |
|------|:------:|------|
| 后退 | `Cmd+[` | 返回上一个访问的目录 |
| 前进 | `Cmd+]` | 前往下一个目录（后退后可用） |
| 导航 | 点击目录 | 清空前进栈，添加到后退栈 |

### 6.4 按钮状态

| 状态 | 后退按钮 | 前进按钮 |
|------|:--------:|:--------:|
| 初始 | 禁用 | 禁用 |
| 已导航 | 启用 | 禁用 |
| 已后退 | 启用/禁用 | 启用 |
| 新导航 | 启用 | 禁用 |

---

## 7. 动画与过渡

### 7.1 动画常量 (AnimationConstants)

#### 标准动画

| 名称 | 时长 | 用途 |
|------|:----:|------|
| `fast` | 0.1s | 快速反馈 |
| `standard` | 0.2s | 默认过渡 |
| `slow` | 0.35s | 强调动画 |

#### 弹簧动画

| 名称 | 参数 | 用途 |
|------|------|------|
| `spring` | response: 0.3, damping: 0.7 | 标准弹簧 |
| `bouncy` | response: 0.4, damping: 0.6 | 活泼弹跳 |
| `stiff` | response: 0.25, damping: 0.85 | 刚性响应 |

#### 交互动画

| 名称 | 时长 | 用途 |
|------|:----:|------|
| `hover` | 0.15s | 悬停效果 |
| `selection` | 0.12s | 选中状态 |
| `buttonPress` | 0.1s | 按钮按压 |

### 7.2 过渡效果 (AppTransitions)

```swift
enum AppTransitions {
    static let fade                    // 淡入淡出
    static let slideFromTop            // 从上滑入
    static let slideFromBottom         // 从下滑入
    static let scaleAndFade            // 缩放淡入
    static let hoverActions            // 悬停操作按钮
    static let messageBanner           // 消息横幅
}
```

### 7.3 View 扩展

```swift
extension View {
    func animateOnHover(_ isHovering: Bool)      // 悬停动画
    func animateSelection(_ isSelected: Bool)   // 选中动画
    func standardAnimation<V>(_ value: V)       // 标准过渡
    func springAnimation<V>(_ value: V)         // 弹簧动画
}
```

---

## 8. 缩略图系统 (ThumbnailCache)

### 8.1 架构

```swift
class ThumbnailCache: ObservableObject {
    static let shared = ThumbnailCache()

    private let cache = NSCache<NSString, NSImage>()  // 内存缓存
    private let taskManager = LoadingTaskManager()    // 异步任务管理

    func loadThumbnail(from urlString: String, maxSize: CGFloat) async -> NSImage?
}
```

### 8.2 缓存策略

| 参数 | 值 | 说明 |
|------|:----:|------|
| 最大数量 | 200 | 缩略图数量限制 |
| 最大容量 | 100MB | 内存占用限制 |
| 分辨率 | 2x | Retina 屏幕适配 |

### 8.3 支持的格式

- JPEG, PNG, GIF, WebP, HEIC
- 自动检测 Content-Type
- 需要公共访问 URL

### 8.4 使用方式

```swift
AsyncThumbnailView(urlString: url, size: 64) {
    // 占位视图
    Image(systemName: "photo.fill")
}
```

---

## 9. 颜色规范 (AppColors)

### 9.1 语义颜色

| 名称 | 用途 | 系统颜色 |
|------|------|----------|
| `windowBackground` | 窗口背景 | `.windowBackgroundColor` |
| `contentBackground` | 内容区背景 | `.controlBackgroundColor` |
| `textPrimary` | 主要文字 | `.labelColor` |
| `textSecondary` | 次要文字 | `.secondaryLabelColor` |
| `textTertiary` | 占位文字 | `.tertiaryLabelColor` |

### 9.2 强调色

| 名称 | 用途 | 系统颜色 |
|------|------|----------|
| `primary` | 主色调/选中 | `.systemBlue` |
| `destructive` | 删除/危险 | `.systemRed` |
| `success` | 成功状态 | `.systemGreen` |
| `warning` | 警告状态 | `.systemOrange` |
| `info` | 信息提示 | `.systemBlue` |

### 9.3 界面元素

| 名称 | 用途 | 系统颜色 |
|------|------|----------|
| `separator` | 分隔线 | `.separatorColor` |
| `border` | 边框 | `.gridColor` |

---

## 10. 排版规范 (AppTypography)

### 10.1 字体规范

| 场景 | 大小 | 权重 |
|------|:----:|------|
| 标题 | 16pt | Semibold |
| 正文 | 13pt | Regular |
| 辅助文字 | 12pt | Regular |
| 小字 | 11pt | Regular |
| 数字 | 12pt | Monospaced Digit |

### 10.2 图标规范

| 场景 | 大小 | 权重 |
|------|:----:|------|
| 工具栏 | 12-13pt | Medium |
| 列表项 | 16pt | Regular |
| 文件类型 | 动态 (0.6x) | Regular |

---

## 11. 组件清单

### 11.1 DesignSystem

| 文件 | 职责 |
|------|------|
| `ViewModeManager.swift` | 视图模式管理 |
| `SelectionManager.swift` | 选择状态管理 |
| `NavigationHistoryManager.swift` | 导航历史管理（前进/后退） |
| `AnimationConstants.swift` | 动画/过渡常量 |
| `ThumbnailCache.swift` | 缩略图缓存 |
| `AppColors.swift` | 颜色定义 |
| `AppTypography.swift` | 字体定义 |
| `AppSpacing.swift` | 间距定义 |

### 11.2 Views

| 文件 | 职责 |
|------|------|
| `FinderToolbar.swift` | 工具栏组件 |
| `PathBar.swift` | 路径导航栏 |
| `FileGridView.swift` | 图标网格视图 |
| `FileGridItemView.swift` | 网格项组件 |
| `FileTableView.swift` | 表格视图 |

---

## 12. 快捷键

### 12.1 导航快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd+[` | 后退（上一个访问的目录） |
| `Cmd+]` | 前进（后退后可用） |
| `Cmd+↑` | 返回上级目录 |
| `Cmd+R` | 刷新列表 |
| `Cmd+F` | 聚焦搜索框 |

### 12.2 视图快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd+1` | 表格视图 |
| `Cmd+2` | 图标视图 |
| `Cmd++` | 放大图标 |
| `Cmd+-` | 缩小图标 |

### 12.3 选择快捷键

| 快捷键 | 功能 |
|--------|------|
| `Cmd+A` | 全选 |
| `Cmd+点击` | 添加/移除选择 |
| `Shift+点击` | 范围选择 |
| `Esc` | 清除选择 |

---

## 13. 深色模式

所有颜色使用 `NSColor` 系统语义颜色，自动适配深色模式：

- 背景色自动调整
- 文字颜色自动反转
- 边框和分隔线适配
- 图标颜色跟随系统

无需额外代码处理深色模式切换。

---

## 相关文档

- [03. 文件导航](./03-file-navigation.md)
- [04. 文件上传](./04-file-upload.md)
- [05. 文件下载与删除](./05-file-download-delete.md)

---

**文档版本**: 1.0
**创建日期**: 2026-01-02
**维护者**: OwlUploader Team
